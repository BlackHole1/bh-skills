interface:
  display_name: "Commit"
  short_description: "Conventional Commits message and commit, English or Chinese body"
  default_prompt: "Use $commit to write a commit message for the current change."
policy:
  allow_implicit_invocation: true
