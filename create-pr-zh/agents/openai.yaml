interface:
  display_name: "Create PR ZH"
  short_description: "Open/update a GitHub PR — English Conventional-Commits title, Chinese Markdown body"
  default_prompt: "Use $create-pr-zh to open (or update) the pull request for the current branch."
policy:
  allow_implicit_invocation: false
